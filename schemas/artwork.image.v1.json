{
  "title": "artwork.image.v1",
  "type": "object",
  "description": "'image' artwork configuration",
  "properties": {
    "image": {
      "$ref": "./image.v1.json"
    },
    "mode": {
      "type": "string",
      "default": "cover",
      "enum": [
        "cover",
        "contain",
        "repeat"
      ]
    },
    "background-color": {
      "$ref": "./color.v1.json"
    }
  },
  "required": [
    "image"
  ]
}